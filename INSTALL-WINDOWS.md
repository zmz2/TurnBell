# TurnBell 1.5.0：Windows + Microsoft Edge 安装、更新与排障

## 一、更新前清理

1. 若右下角仍有旧版 EXE 托盘图标，右键退出，以后不再运行旧 EXE。
2. 打开 `edge://extensions/`。
3. 确认最终只保留一个 TurnBell 或旧“GPT 回复完成提醒器”。两个实例会造成重复提示音和通知。

TurnBell 1.5.0 不安装 EXE、Windows 服务、驱动、计划任务、开机启动项或本地端口。

## 二、安装或更新

### 1. 完整解压

不要直接在 ZIP 预览窗口运行。解压后双击：

```text
Install-Or-Update-Current-Edge.cmd
```

脚本会：

- 优先检测 `%LOCALAPPDATA%\GPTReplyNotifier\extension`；
- 若旧目录存在，就在原路径执行镜像更新，尽量保留扩展 ID；
- 全新安装使用 `%LOCALAPPDATA%\TurnBell\extension`；
- 使用 `/MIR` 删除旧版本遗留文件；
- 打开固定目录和 `edge://extensions/`。

### 2. 在 Edge 重新加载

- 已安装旧版：在原扩展卡片点击“重新加载”；
- 首次安装：开启“开发人员模式” → “加载解压缩的扩展” → 选择脚本显示的固定目录。

确认版本为：

```text
1.5.0
```

### 3. 刷新 ChatGPT 标签页

把更新前已经打开的 `chatgpt.com` / `chat.openai.com` 标签页全部刷新一次。刷新不会主动清除 Cookie、退出登录或删除对话。

1.5.0 也修复了后台给现有标签页补注入时漏掉 `bootstrap-core.js` 的问题，但更新旧内容脚本最可靠的方法仍是刷新页面。

## 三、先验证历史页面不再误报

1. 打开已有对话；
2. 刷新页面；
3. 等待历史内容加载完成；
4. 预期：**不播放声音、不弹通知、不出现完成徽标**。

TurnBell 会等待页面结束 `loading`，并在 DOM 连续安静约 2 秒后把现有内容设为静默基线。

## 四、验证新消息

1. 在同一页面的输入框按 Enter 发送一个需要数秒回答的问题；
2. 立即切换到同一 Edge 窗口的其他标签页；
3. 中间推理、工具进度或阶段性摘要不应提示；
4. 最终回答出现且不再变化，下面出现复制/朗读/赞踩等操作栏后，应最多提醒一次；
5. 对应标签页的 TurnBell 图标应出现 `✓`。

Shift+Enter 和中文输入法组合态不会被当成发送。

### 验证 Instant / “极速”

1. 在 ChatGPT 中选择 Instant/极速；
2. 用普通 Enter 发送一个简短问题；
3. 立即切到同一 Edge 窗口中的其他标签页；
4. 若该路径没有展示生成态或最终操作栏，TurnBell 会在最终文本连续稳定约 3 秒后提示；
5. 预期仍然是每轮最多一次。

该兼容路径必须由真实的 Enter、发送按钮或重试动作启动。刷新加载的历史内容不会获得这种资格。

## 五、选择 Windows 默认通知声

打开 TurnBell 弹窗：

1. 保持“提示音”开启；
2. 在“提示音样式”选择 `Windows 默认通知声（推荐）`；
3. 点击“测试所选通知通道”。

此模式不会播放 TurnBell 自带 WAV，声音由 Windows/Edge 的通知设置决定。音量滑块会禁用，因为系统声响度由 Windows 控制。

若改选柔和双音、温暖铃声等内置音效，TurnBell 会静音系统通知声，只播放一次所选本地音效，避免双重声音。

## 六、测试系统通知

打开 TurnBell 弹窗，在“系统通知通道”中逐个测试。

### 方案 A：Edge 扩展通知 API（官方通道）

1. 选择 `Edge 扩展通知 API（官方通道）`；
2. 点击“测试所选通知通道”；
3. 阅读下方诊断。

可能结果：

- `已接受并保持活动`：Edge 创建并保留了通知对象；不等于 Windows 一定显示横幅。
- `已创建，未保持活动`：Edge/Windows 可能立即收纳或关闭了对象。
- `权限被拒绝`：扩展通知权限不可用。
- `创建失败`：弹窗会显示 API 错误。

### 方案 B：Web 通知兼容通道（实验）

1. 切换到 `Web 通知兼容通道（实验）`；
2. 再点击测试；
3. 查看是否出现 Windows 横幅以及诊断文字。

该通道使用扩展 Service Worker 的 Web Notification 接口。它不会和官方通道同时发送，因此不会因为测试设置本身造成双通知。它仍受 Edge、Windows 和组织策略控制。

## 七、Windows 设置里没有 Microsoft Edge

双击：

```text
Open-Windows-Notification-Settings.cmd
```

检查：

1. Windows 通知总开关；
2. “请勿打扰”与通知中心；
3. `edge://policy` 中的：

   ```text
   AllowSystemNotifications
   ```

   - `True` 或未设置：Edge 被允许使用系统通知；
   - `False`：Edge 使用内置消息中心回退。

Windows 应用列表中没有 Edge，不能由扩展自行修复。若两个 TurnBell 通道都报告成功而 Windows 仍没有横幅，说明限制发生在 Edge/Windows 展示层。纯扩展不能绕过这一层；此时提示音和工具栏 `✓` 是可用回退。

## 八、仍然是“刷新历史有提示，新消息没提示”

按顺序检查：

1. 扩展卡片版本必须是 `1.5.0`；
2. `edge://extensions/` 中只能有一个 TurnBell/旧提醒扩展；
3. 点击扩展卡片“重新加载”；
4. 刷新发生问题的 ChatGPT 标签页；
5. 打开弹窗，确认“DOM 完成监听已接管 N 个 ChatGPT 标签页”；
6. 在聊天框用普通 Enter 发送；
7. 最终回答下方确认出现复制、朗读或赞踩等最终操作控件。

1.5.0 已覆盖以下回归：

```text
空页面 → 历史对话逐步水合 → 静默基线
按 Enter 发送 → 新轮次立即武装
生成中 → 文字变化 → 最终操作栏出现
静默确认 → 只发送一次完成候选
按 Enter 发送极速请求 → 无生成态/无操作栏 → 稳定约 3 秒 → 一次完成候选
```

## 九、连续提问

1. 第一轮完成并收到一次提醒；
2. 不刷新，立即发送第二轮；
3. 第二轮最终完成后再提醒一次；
4. 第一轮迟到的 DOM 变化不应再次提醒。

## 十、只有声音，没有横幅

这说明检测器、后台消息和离屏音频链路至少已经工作。继续检查：

- 当前选中的通知通道及其诊断；
- `AllowSystemNotifications`；
- Windows “请勿打扰”；
- `Win + N` 通知中心；
- Edge 是否使用内置消息中心。

## 十一、系统通知有横幅，但声音不对或没有声音

检查：

- “提示音”开关；
- 音效主题是否为 `Windows 默认通知声（推荐）`；
- Windows 默认声只能通过“测试所选通知通道”试听，不能用内置音效的“试听”按钮；
- Windows “设置 → 系统 → 声音”中的系统声音与音量混合器；
- Windows 音量混合器中的 Edge；
- 浏览器是否被静音。

若选择的是某个内置音效，再检查音效“试听”和音量滑块。内置音效与 Windows 默认声不会同时播放。

## 十二、不同 Edge 配置 / InPrivate

- “个人”“学校”等不同 Edge 配置需分别加载扩展。
- InPrivate 需要在扩展详情页单独允许。
- 受组织管理的 Edge 可能禁止开发者扩展或系统通知；TurnBell 不绕过管理策略。

## 十三、卸载

1. 在 `edge://extensions/` 删除 TurnBell；
2. 删除实际使用的一个或两个目录：

   ```text
   %LOCALAPPDATA%\GPTReplyNotifier
   %LOCALAPPDATA%\TurnBell
   ```

3. 删除旧 EXE 文件（若仍保留）。
