# Changelog

## 1.5.1 — 2026-09-19

### 可靠性

- 路由切换、窗口失焦或锁屏期间的完成状态使用可恢复通知队列；通知创建失败后由后台闹钟重试，Service Worker 重建后也会恢复队列和 watchdog。
- 同一轮的重试通知复用稳定 ID，已活动的通知不会被重复创建或重复播放声音。
- 解锁时保留仍活动的锁屏通知；仅当初始通知已经消失时才补发通用提醒。
- 修复占位新对话延迟获得 `/c/<id>` 路由时丢失轮次，以及延迟路由响应覆盖新轮次的问题。
- 路由恢复保留原助手基线，并允许有生成态或页面内指纹变化作为强证据的同数量重试回答。
- 快速切换会话时将旧路由 DOM 隔离到其所属路径；往返导航仍能恢复待完成回答，新路由的显式发送也不会被旧节点挡住。
- 路由握手失败会退避重试；候选回复跨路由恢复时保留并更新路由代次，未完成的旧轮次在同一路由开启新轮次后仍可收尾。
- Web Notification 点击会等待状态确认；通知数据不再带会话 URL。
- 解锁与初始通知创建并发时以最新队列状态领取重放；用户关闭通知遇到暂时存储错误时会重试确认和成对清理。
- 修改无关设置不会重置已明确武装的检测轮次。

### 隐私与验证

- 最终化存储到期记录会清理；通知待发状态只保存不含标题、URL、正文和指纹的元数据。
- 修正候选消息隐私测试的函数范围，使断言检查真实候选构造代码。
- 更新版本号并新增后台重启、通知失败重试和锁屏解锁回归。

## 1.5.0 — 2026-08-17

### 可靠性

- 新增启动水合门控：页面历史对话加载完成并连续安静 2 秒后只建立静默基线，不再把刷新后的旧回答当成新完成。
- 普通 Enter 在 ChatGPT 输入区会显式武装新轮次；Shift+Enter、组合输入和修饰键不会误触。
- 修复后台给已经打开的标签页补注入时遗漏 `bootstrap-core.js`，导致内容脚本依赖检查失败后静默退出的问题。
- 新增完整生命周期回归：历史水合 0 次提醒，普通 Enter 新发消息最终完成 1 次候选，随后 Instant/极速轮次也只产生 1 次候选。
- 新增 Instant/极速保守兼容路径：仅对明确 Enter/发送动作启动、从未出现生成态、回答相对基线发生变化且连续稳定约 3 秒的轮次，允许在缺少最终操作栏时完成。
- Instant 兼容证据不会授予刷新历史、隐式 DOM 变化或曾出现推理/生成态的轮次，防止重新引入历史和推理摘要误报。
- 识别没有角色标记、且最终操作栏位于轮次后方的实时助手回复。
- 外部最终操作栏只归属最近的前置轮次，避免一个操作栏匹配多个历史轮次。

### 通知

- 保留官方 `chrome.notifications` 通道。
- 新增可选的 Service Worker Web Notification 兼容通道，用于排查部分 Edge/Windows 路由差异。
- 两个通道在界面中互斥选择，避免因双通道同时发送造成重复横幅。
- 弹窗分别报告扩展通知与 Web 兼容通道的支持、创建和活动状态。
- 默认音效改为 Windows/Edge 当前系统通知声；通知使用非静音模式，不再叠加扩展 WAV。
- 自定义音效模式会静音系统通知声，仅播放一次本地 WAV；关闭声音时两类声音都关闭。
- 旧版声音设置通过一次性迁移切换到 Windows 默认声，迁移完成后保留用户后续主动选择。

### 文档与测试

- 安装说明加入“刷新历史不提示、新消息提示”的逐步验收。
- 明确说明纯扩展无法强制 Windows 展示横幅或绕过 `AllowSystemNotifications`。
- 自动化测试扩展到历史水合、Enter 发送、补注入依赖、外部操作栏归属和 Web 通知通道。

## 1.4.0 — 2026-08-16

### 可靠性

- 合并多组 ChatGPT DOM 选择器，不再命中第一种布局后提前停止。
- 将同一轮中相互嵌套的助手/用户节点归并到共同轮次容器，防止重复计数。
- 从真实外层轮次容器及其紧邻后置区域查找复制、朗读、赞踩等最终操作控件，修复正文节点找不到操作栏导致的漏报。
- 按页面真实文档顺序选择最新轮次，并在同组中采用最完整文本。
- 新轮次键加入对话路径、用户消息身份/指纹、助手数量和检测周期。
- 发送、重新生成、重试和继续生成等显式用户操作会直接武装检测周期，作为页面选择器缺失时的第三条启动路径。
- 修复新一轮开始事件漏失后，上一轮状态永久压制后续通知的问题。
- 使用候选真实开始时间和用户轮次数区分新轮次与迟到旧事件。
- 后台标签页静默复核保持消息通道存活；隐藏页采样使用微任务降低计时器节流影响。
- 默认长任务等待上限提高到 2 小时，内部允许最高 8 小时。
- 继续坚持每轮至多一次；最终证据不足时不通过超时猜测完成。

### 安全与隐私

- 删除网页内部响应流监听、SSE 元数据解析和网络生命周期模块。
- 删除 `webRequest`、`alarms` 及通配子域名权限。
- 不再注入页面主世界，不包装 `fetch` / `XMLHttpRequest`。
- 运行权限缩减为 `notifications`、`storage`、`offscreen`、`scripting`。
- 站点权限缩减为 `chatgpt.com` 和 `chat.openai.com`。
- 回复正文仅在当前页面进程中用于稳定性判断和短指纹，不传给后台、不存储、不上传。
- 发布校验明确拒绝旧拦截模块、网络包装标记、EXE 和额外权限。
- 新增 `PRIVACY.md`。

### 通知与音效

- 系统通知增加权限、创建结果和活跃对象诊断。
- 增加每标签页工具栏 `✓` 徽标，点击通知或激活标签页后清除。
- 系统通知、声音和徽标成为三条独立反馈通道。
- 新增五种本地音效：柔和双音、温暖铃声、玻璃轻响、轻柔气泡、数字提示。
- 支持音效试听、音量调整和持续通知选项。
- 新增 Windows 通知设置与 Edge `AllowSystemNotifications` 策略快捷检查脚本。

### 安装

- 已存在旧固定目录时原路径镜像更新，尽量保留 Edge 未打包扩展 ID并避免重复安装。
- 全新安装使用 `%LOCALAPPDATA%\TurnBell\extension`。
- 安装脚本使用 `/MIR` 清除旧版残留文件。

## 1.3.0 — 2026-08-16

- 增加后台标签页完成检测和每轮去重。
- 移除 Windows EXE 依赖，改用 Edge 扩展通知。
- 该版本曾使用网页内部响应流辅助判断；此路径已在 1.4.0 完全移除。

## 1.2.0 — 2026-08-16

- 引入 Service Worker 后台复核和隐藏标签页调度。
- 移除对本地通知助手端口的依赖。
